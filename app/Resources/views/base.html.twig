<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        <title>{% block title %}No dzień dobry!{% endblock %}</title>

        {% block stylesheets %}
            <link rel="stylesheet" href="{{ asset("css/bootstrap.min.css") }}"/>
        {% endblock %}
    </head>
    <body>
        {% block sidebar %}
            <div class="container ">
                {% if is_granted("ROLE_USER") %}
                    {% include("menuAuthenticated.html.twig") %}
                {% else %}
                    {% include("menuAnonymous.html.twig") %}
                {% endif %}
            </div>
        {% endblock %}
        <div id="content">
            <div class="container pt-4">
            {% block body %} {% endblock %}
            </div>
        </div>
        {% block javascripts %}{% endblock %}
    </body>
</html>
